скачать линукс (
макрдаун файлы
установка 
используем менеджер пакетов и устанавливаем через apt 

установить python
установить pycharm
установить venv и pip

d
mkdir - создать папку
touch создать файл
-m запусти следующий аргкмент как модуль
-cd change directory
ls выдает не скрытые директории a появляются скрытые директории (скрытые - те директории которые начинаются с точки) l - лист
ls la список всех файлов
. текущая директория
.. предыдущая директория
sourse (путь до активации в .venv) - активировать venv чтобы дективироаьт просто прописываем deactivate 
whoami | python main.py | - передает данные на вход в другую команду
Grep ищет по шаблону что то в потоке вывода
> создает файл и можно перенаправить поток вывода в этот файл
2> стандартный поток вывода ошибок 
&> весь поток ввода выводится в файл
если мы хотим чтобы информация не перезапичывалась а дописывалась то испольхуем >>

docker контейниры, 
расширения нужны редактором кода, чтобы подсвечивались например подсказки python и без этотго все запустит

2 варианта работы питона
пакетный и интерактивный(все что мы вводим оно выполняется сразу например, используется для тестирование иногда, чтобы зайти lession python
выйти - exit()

принт - систем аут
echo '11111

можем передавать в команду флпги и они начигаеются с - флаг с аргументов типа -version=1234 где 1234 это аргумент

python main.py && python broke_main.py
2 файл начнет свое выполнение только есои 1 файл зкончит свое выполнение без ошибок 
|| или выполнение с обиками

маркдаун файлы

у виндовс есть проблемы с многопроцесерностью установкой зависимостей и пакетов
невысоконагруженные веб сервисы ИИ проверка серверов